/** 동아리 프로필/로고 이미지가 없을 때 사용하는 기본 이미지 (라이트 모드) */
export const DEFAULT_CLUB_IMAGE_LIGHT = '/images/default-club.svg';

/** 동아리 프로필/로고 이미지가 없을 때 사용하는 기본 이미지 (다크 모드) */
export const DEFAULT_CLUB_IMAGE_DARK = '/images/default-club-dark.svg';

/** @deprecated 라이트/다크 분리용으로 DEFAULT_CLUB_IMAGE_LIGHT 또는 DEFAULT_CLUB_IMAGE_DARK 사용 */
export const DEFAULT_CLUB_IMAGE = DEFAULT_CLUB_IMAGE_LIGHT;
